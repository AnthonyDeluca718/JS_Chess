import merge from 'lodash/merge';

let _emptyBoard = {};

const BoardReducer = function(state = _emptyState) {
  switch(action.type) {
    default:
      return state;
  }
}

export default BoardReducer;
